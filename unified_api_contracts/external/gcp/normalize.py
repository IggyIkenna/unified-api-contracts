"""GCP normalizers — infra types to canonical.

Removed: GCS->CanonicalCloudStorage, Compute->CanonicalVirtualMachine,
Artifact Registry->CanonicalContainerRegistry, Cloud Build->CanonicalComputeJob
(speculative infra types deleted).
"""

from __future__ import annotations

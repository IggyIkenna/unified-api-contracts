# STUB — LOCAL-SIDECAR
# tardis-machine stream-normalized WS runs as a local Node sidecar (ws://localhost:8001/ws-stream-normalized).
# Connector: market_tick_data_service/live/connectors/tardis_machine_ws.py
# Status: opt-in via --live-source tardis-machine; not recorded in CI (no live sidecar in test env).
ws_url: ws://localhost:8001/ws-stream-normalized
subscription: null
frames: []
version: 1

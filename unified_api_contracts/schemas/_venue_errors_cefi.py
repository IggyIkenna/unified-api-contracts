"""Venue error classifications for CeFi exchanges."""

from __future__ import annotations

from unified_api_contracts.schemas._errors_types import ErrorAction, VenueErrorClassification, ve

VENUE_ERRORS_CEFI: dict[str, list[VenueErrorClassification]] = {
    "binance": [
        ve(
            "binance",
            "-1003",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Too many requests",
        ),
        ve(
            "binance",
            "-1021",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Timestamp recv window",
        ),
        ve(
            "binance",
            "-1000",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Unknown error",
        ),
        ve(
            "binance",
            "-1006",
            retry=True,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="Connection closed",
        ),
        ve(
            "binance",
            "-2011",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Unknown order",
        ),
        ve(
            "binance",
            "-1013",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Invalid lot size",
        ),
    ],
    "bybit": [
        ve(
            "bybit",
            "10006",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit exceeded",
        ),
        ve(
            "bybit",
            "10000",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Internal error",
        ),
        ve(
            "bybit",
            "10001",
            retry=True,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="Parameter error",
        ),
        ve(
            "bybit",
            "10019",
            retry=True,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="WS restarting",
        ),
        ve(
            "bybit",
            "33004",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="API key invalid",
        ),
    ],
    "okx": [
        ve(
            "okx",
            "50004",
            retry=True,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="WebSocket closed",
        ),
        ve(
            "okx",
            "50011",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit exceeded",
        ),
        ve(
            "okx",
            "50026",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="System busy",
        ),
        ve(
            "okx",
            "51008",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Insufficient funds",
        ),
    ],
    "deribit": [
        ve(
            "deribit",
            "10028",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Too many requests",
        ),
        ve(
            "deribit",
            "10040",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Too many requests",
        ),
        ve(
            "deribit",
            "11044",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Not enough funds",
        ),
        ve(
            "deribit",
            "13009",
            retry=False,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="Invalid token",
        ),
        ve(
            "deribit",
            "13010",
            retry=False,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="Token revoked",
        ),
    ],
    "coinbase": [
        ve(
            "coinbase",
            "RATE_LIMIT_EXCEEDED",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit",
        ),
        ve(
            "coinbase",
            "INTERNAL_SERVICE_ERROR",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Server",
        ),
        ve(
            "coinbase",
            "TEMPORARILY_UNAVAILABLE",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Temp",
        ),
    ],
    "tardis": [
        ve(
            "tardis",
            "401",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Unauthorized",
        ),
        ve(
            "tardis",
            "429",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit exceeded",
        ),
        ve(
            "tardis",
            "500",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Server error",
        ),
    ],
    "alchemy": [
        ve(
            "alchemy",
            "-32600",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Invalid request",
        ),
        ve(
            "alchemy",
            "-32603",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Internal error",
        ),
        ve(
            "alchemy",
            "429",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit exceeded",
        ),
    ],
    "yahoo_finance": [
        ve(
            "yahoo_finance",
            "RATE_LIMIT_EXCEEDED",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate",
        ),
        ve(
            "yahoo_finance",
            "429",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Too many requests",
        ),
    ],
    "upbit": [
        ve(
            "upbit",
            "invalid_access_key",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Invalid key",
        ),
        ve(
            "upbit",
            "invalid_query_payload",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Invalid",
        ),
        ve(
            "upbit",
            "too_many_requests",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit",
        ),
    ],
    "ibkr": [
        ve(
            "ibkr",
            "100",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Market data farm",
        ),
        ve(
            "ibkr",
            "1100",
            retry=True,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="Connectivity lost",
        ),
        ve(
            "ibkr",
            "1300",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Order rejected",
        ),
    ],
    "thegraph": [
        ve(
            "thegraph",
            "Subgraph not found",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Not found",
        ),
        ve(
            "thegraph",
            "429",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit exceeded",
        ),
    ],
    "ccxt": [
        ve(
            "ccxt",
            "RateLimitExceeded",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit",
        ),
        ve(
            "ccxt",
            "ExchangeNotAvailable",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Unavail",
        ),
        ve(
            "ccxt",
            "ExchangeError",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Transient",
        ),
    ],
    "databento": [
        ve(
            "databento",
            "RATE_LIMIT",
            retry=True,
            reconnect=False,
            action=ErrorAction.RETRY,
            desc="Rate limit",
        ),
        ve(
            "databento",
            "AUTH_FAILURE",
            retry=False,
            reconnect=False,
            action=ErrorAction.FAIL,
            desc="Auth failed",
        ),
        ve(
            "databento",
            "CONNECTION_RESET",
            retry=True,
            reconnect=True,
            action=ErrorAction.RECONNECT,
            desc="Reset",
        ),
    ],
}

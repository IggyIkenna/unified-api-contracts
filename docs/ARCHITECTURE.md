# Architecture

**SSOT for constraints and layout:** [`unified-trading-pm/codex/02-data/contracts-scope-and-layout.md`](../../unified-trading-pm/codex/02-data/contracts-scope-and-layout.md). **Full layout:** [PACKAGE_LAYOUT_AND_SCOPE.md](PACKAGE_LAYOUT_AND_SCOPE.md). **Workspace post-plan reality:** [`unified-trading-pm/codex/POST_PLAN_REALITY_2026_05_06.md`](../../unified-trading-pm/codex/POST_PLAN_REALITY_2026_05_06.md).

## UAC's role in the workspace post-plan reality

UAC is the **single source of truth for canonical schemas, registries, and contracts** that the rest of the workspace consumes. Per the writegate-honest-coverage plan + predictions plan landing 2026-05-06, UAC owns these new SSOT registries (in addition to its existing schemas/registry/external content):

| Registry / SSOT                                                                                     | Module                                                           | Purpose                                                                                                                                                                                                                                                                                                                                         |
| --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BUNDLED_DATA_TYPES`                                                                                | `canonical/crosscutting/honest_coverage.py`                      | Set of data_types where `ManifestWriter.record_captured` requires `expected_root_clusters` + `cluster_extractor` (UTL guard raises `MissingClusterValidationError` if absent; QG STEP 5.64 statically checks). Includes `options_chain`, `futures_chain`, `prediction_canonical_question_group`, `ODDS_SNAPSHOT`, `ODDS_MOVEMENT`, `ARBITRAGE`. |
| `DATA_TYPE_TO_CLUSTER_REGISTRY`                                                                     | `canonical/crosscutting/honest_coverage.py`                      | data_type → registry symbol name (e.g. `"options_chain"` → `"OPTIONS_CLUSTERS"`)                                                                                                                                                                                                                                                                |
| `OPTIONS_CLUSTERS`                                                                                  | `canonical/crosscutting/honest_coverage.py`                      | ES.OPT 11-cluster taxonomy lifted from instruments-service to UAC                                                                                                                                                                                                                                                                               |
| `FUTURES_CLUSTERS`                                                                                  | `canonical/crosscutting/honest_coverage.py`                      | ES + MES seeds; per-root spreads/butterflies; `derive_expiry_bucket` UTL helper                                                                                                                                                                                                                                                                 |
| `SPORTS_FIXTURE_CLUSTERS`                                                                           | `canonical/crosscutting/honest_coverage.py`                      | Per-league-tier expected bookmaker sets for `ODDS_*` data_types                                                                                                                                                                                                                                                                                 |
| `PREDICTION_GROUPS`                                                                                 | `canonical/crosscutting/honest_coverage.py`                      | Per-canonical-group expected market_ids per day by cadence (HOURLY=24/day, DAILY=1/day, ELECTION=1 over months); populated by predictions Plan A; empty placeholder until then                                                                                                                                                                  |
| `SOURCE_PRIORITY`                                                                                   | `canonical/crosscutting/source_priority.py`                      | `(asset_group, data_type) → list[source_key]` — top-entry per pair drives `available_at` stamping latency. Multi-source merge tie-breakers (timestamp-availability > coverage > info-richness > merge-different-fields) deferred to Plan D.                                                                                                     |
| `AVAILABILITY_AT_SEMANTICS`                                                                         | `canonical/crosscutting/availability_semantics.py`               | `(asset_group, data_type)` → semantic enum (`fetch_completed_at` / `kickoff_minus_60min` / `match_end_time` / `event_time` / `report_time` / `announced_at` / `forecast_issue_time` / `tick_plus_latency`). Drives UTL `availability_stamping.stamp_available_at_*` helpers per row.                                                            |
| `CanonicalQuestionGroup` enum + `classify_market_to_canonical_group`                                | `canonical/domain/predictions/{canonical_groups,classifiers}.py` | Predictions Plan A: maps Polymarket conditionId / Kalshi ticker → canonical group via word-boundary regex + classifier stability hash.                                                                                                                                                                                                          |
| `MarketLifecycle` dataclass                                                                         | `canonical/domain/predictions/lifecycle.py`                      | Per-market `market_created_at` / `resolution_time` / `settlement_time` / `current_status`. MTDS respects bounds; LookaheadBiasError per-market-aware.                                                                                                                                                                                           |
| `MATCH_END_TIME_DETECTORS`                                                                          | `canonical/domain/sports/`                                       | Detection cascade for sports `match_end_time`: api_football native → SFI progressive-stats freeze → footystats → understat → low-confidence `kickoff + 120min` fallback.                                                                                                                                                                        |
| `venue_trading_calendar` (existing, called out)                                                     | `canonical/domain/market/`                                       | Non-trading days pre-skipped → `empty_confirmed`.                                                                                                                                                                                                                                                                                               |
| `SOURCE_COVERAGE_START` / `DATA_TYPE_COVERAGE_START` / `KNOWN_COVERAGE_GAPS` (existing, called out) | `canonical/domain/sports/`                                       | Denominator clipping for honest coverage %.                                                                                                                                                                                                                                                                                                     |

These registries land in writegate Phase 1B + predictions Plan A Phase 1A. Consumers (UTL, MTDS, MDPS, instruments-service, features-\*, deployment-api) import from these UAC modules; no service may declare its own copy.

## Package structure

The package is organized into six top-level directories plus root facade files:

| Directory              | Purpose                                                                 | Contents                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Root facade files**  | Domain-level re-exports for consumer convenience                        | `market.py`, `execution.py`, `reference.py`, `sports.py`, `position.py`, `features.py`, `derivatives.py`, `infrastructure.py`, `connectivity.py`, `latency.py`, `options.py`, `odds.py`, `errors.py`, `rate_limits.py`, `sports_reference.py`, **`predictions.py`** (predictions Plan A facade re-exporting `CanonicalQuestionGroup` + `classify_market_to_canonical_group` + `MarketLifecycle` + `is_market_active_at`) |
| **`canonical/`**       | Canonical domain types, crosscutting types, errors                      | `domain/{market,execution,reference,sports,position,features,derivatives,infrastructure,onchain,predictions}/`, `crosscutting/{analytics,connectivity,latency,rate_limits,risk,honest_coverage,source_priority,availability_semantics}/`, `errors/{cefi,defi,altdata,sports}`, `canonical_mappings.py`                                                                                                                   |
| **`external/`**        | Raw per-source request/response/error schemas + per-source normalize.py | 79 source directories (binance, databento, hyperliquid, betfair, fix, gcp, etc.)                                                                                                                                                                                                                                                                                                                                         |
| **`normalize_utils/`** | Shared normalization helpers used by per-source normalize.py files      | 25 modules (ohlcv, tickers, orderbooks, trades, symbols, instruments, options, sports, onchain, etc.)                                                                                                                                                                                                                                                                                                                    |
| **`registry/`**        | Venue/capability/endpoint registry and manifest                         | `capability.py`, `capability_data.py`, `endpoint_registry.py`, `venue_constants.py`, `venue_rate_limits.py`, `venue_manifest/{cefi,defi,tradfi,betting_sports,data_providers,internal_services}.py`                                                                                                                                                                                                                      |
| **`config/`**          | Validation configuration                                                | `trading_validation.py`                                                                                                                                                                                                                                                                                                                                                                                                  |
| **`testing/`**         | Test infrastructure exported for consumers                              | `conftest_helper.py`, `fault_injection.py`, `detect_cassette_drift.py`, `network_block_plugin.py`, `vcr_endpoints.py`                                                                                                                                                                                                                                                                                                    |

**New post-2026-05-06**: `canonical/crosscutting/honest_coverage.py` + `source_priority.py` + `availability_semantics.py` + `canonical/domain/predictions/{canonical_groups,classifiers,lifecycle}.py`.

## Placement rule (where new modules go)

| Content type                                            | Location                           | Examples                                                                                                                  |
| ------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **Raw schemas for an external API, protocol, or venue** | **`external/<name>/`**             | binance, databento, ccxt, fix, nautilus, prime_broker, regulatory                                                         |
| **Normalization for a specific source**                 | **`external/<name>/normalize.py`** | Each source's normalize.py maps raw schemas to canonical types                                                            |
| **Shared normalization helpers**                        | **`normalize_utils/`**             | ohlcv.py, tickers.py, orderbooks.py, symbols.py                                                                           |
| **Canonical domain types**                              | **`canonical/domain/<domain>/`**   | market, execution, sports, position, features, derivatives, reference, infrastructure, onchain, **predictions**           |
| **Canonical crosscutting types**                        | **`canonical/crosscutting/`**      | analytics, connectivity, latency, rate_limits, risk, **honest_coverage**, **source_priority**, **availability_semantics** |
| **Canonical error types**                               | **`canonical/errors/`**            | cefi.py, defi.py, altdata.py, sports.py                                                                                   |
| **Root facade re-exports**                              | **Root `.py` files**               | `market.py`, `execution.py`, **`predictions.py`** -- re-export from canonical for convenience                             |
| **Venue/capability/endpoint metadata**                  | **`registry/`**                    | capability.py, venue_constants.py, endpoint_registry.py                                                                   |
| **Venue manifest data**                                 | **`registry/venue_manifest/`**     | cefi.py, defi.py, tradfi.py, betting_sports.py                                                                            |

**Internal** (service-to-service) schemas live in **unified-internal-contracts**; UAC is external + normalised + canonical-crosscutting only.

## External vs normalised vs canonical-crosscutting

- **External**: Venue- or protocol-specific Pydantic models matching provider APIs. Use for parsing and validating raw API responses before mapping to canonical types. Each `external/{source}/` directory contains `schemas.py` and `normalize.py`.
- **Normalised**: Canonical types used by MTDS / UMI / UOI and services. Organized under `canonical/domain/` by domain (market, execution, sports, predictions, etc.) and `canonical/crosscutting/` for cross-domain concerns. Root facade files (`market.py`, `execution.py`, `predictions.py`, etc.) re-export these for convenience.
- **Canonical-crosscutting (post-2026-05-06)**: workspace-wide registries that drive runtime behaviour at write-time and read-time. `honest_coverage.BUNDLED_DATA_TYPES` + `cluster registries` + `source_priority.SOURCE_PRIORITY` + `availability_semantics.AVAILABILITY_AT_SEMANTICS` are consumed by UTL `ManifestWriter.record_captured`, UTL `availability_stamping.stamp_available_at_*`, deployment-api data-status, and all per-service adapters.

## Cross-references

- **Canonical post-plan summary + active plans + 10 cross-cutting principles**: [`unified-trading-pm/codex/POST_PLAN_REALITY_2026_05_06.md`](../../unified-trading-pm/codex/POST_PLAN_REALITY_2026_05_06.md)
- **Manifest semantics SSOT**: [`unified-trading-pm/codex/02-data/availability-manifest-and-data-status.md`](../../unified-trading-pm/codex/02-data/availability-manifest-and-data-status.md)
- **Live = batch contract** (UAC-specific implications): [BATCH_LIVE_SYMMETRY.md](BATCH_LIVE_SYMMETRY.md)
- **Schema governance** (UAC-specific placement + ownership rules): [SCHEMA_GOVERNANCE.md](SCHEMA_GOVERNANCE.md)
- **Active plans**: [`writegate_honest_coverage_endtoend_2026_05_06.md`](../../unified-trading-pm/plans/active/writegate_honest_coverage_endtoend_2026_05_06.md), [`predictions_canonical_question_group_polymarket_migration_2026_05_06.md`](../../unified-trading-pm/plans/active/predictions_canonical_question_group_polymarket_migration_2026_05_06.md)
- **README structure**: [README Structure](../README.md#structure), [docs/INDEX.md](INDEX.md), [docs/CROSS_VENUE_MATRIX.md](CROSS_VENUE_MATRIX.md).
